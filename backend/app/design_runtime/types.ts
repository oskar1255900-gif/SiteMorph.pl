export type CTA = { label: string; href: string; kind?: 'primary' | 'secondary' | 'ghost' };
export type MediaRef = { src?: string; alt: string; aspect?: string; focalPoint?: { x: number; y: number }; mobileFocalPoint?: { x: number; y: number }; treatment?: string; credit?: string; source?: string };
export type SectionProps = { id: string; primitive: string; role: string; props: Record<string, any>; tone: string; container: string; signature: boolean; rhythm: { density: string; containment: string; paceRole: string } };
export type SiteConfig = { businessBrief: { name: string; category: string }; creative: { heroFamily: string }; pagePlan: { sections: SectionProps[] }; interactions: { nav: { itemLabel: string; targetSectionId: string }[]; links: CTA[]; forms: { id: string; mode: string; email?: string }[] }; mobile: { reduceParallax: boolean }; tokens: { motionStrategy: { heroReveal: string; sectionReveal: string; presetId: string; intensity: string } }; assetCredits: { name: string; url: string }[] };
